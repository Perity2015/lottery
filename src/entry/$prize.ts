import { PrizeLevel } from "../helper/const"

export class $Prize {
	public key!: PrizeLevel;
	public name!: string;
	public per!: number;
	public style!: any;
	public title!: string;
	public total!: number;
}